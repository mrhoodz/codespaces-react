import type { Plugin } from "vite";
import type { FilterPattern } from "@rollup/pluginutils";
export interface MacroReplaceLocation {
    lo: number;
    hi: number;
    import_src: string;
    import_name: string;
}
export interface MacroRemoveLocation {
    lo: number;
    hi: number;
}
export interface MacroOutput {
    replaces: MacroReplaceLocation[];
    removals: MacroRemoveLocation[];
}
export type MacroFilter = (ident: string, id: string, importer: string) => boolean;
export interface MacroPluginOptions {
    preset?: "pandacss" | undefined;
    filter?: MacroFilter;
    assertType?: string;
    include?: FilterPattern | undefined;
    exclude?: FilterPattern | undefined;
    rootDir?: string;
}
export declare const macroPlugin: (opts?: MacroPluginOptions) => Promise<Plugin>;
